@import('web.components.code-block')

<template>
    @extends(__layout__ + 'lab')
    @section('meta:title', 'Ví dụ Thực tế (Examples) - Saola Ecosystem')
    @section('meta:description', 'Tuyển tập các ứng dụng web thực tế xây dựng trên nền tảng Saola: Data Grid 3.800 dòng, Kanban kéo thả, Modal CRUD, Realtime Ticker, Event Stream SSE, Roster Sync và Todo Workspace.')
    @section('meta:keywords', 'saola examples, vi du thuc te, kanban board, data grid, realtime market, event stream, roster sync, modal crud')

    @block('main')
        <div class="ex-hero">
            <p class="kicker"><span>ỨNG DỤNG THỰC TẾ · SHOWCASE</span></p>
            <h1>Tuyển tập Ví dụ Ứng dụng Thực tế</h1>
            <p class="lab-lead">
                Không phải là những đoạn code demo cú pháp nhỏ lẻ, các ví dụ dưới đây là những mô hình bài toán thực tế
                thường gặp khi xây dựng ứng dụng web hiện đại: xử lý dữ liệu lớn, tương tác kéo thả, đồng bộ ngầm đa người dùng,
                và kết nối thời gian thực — tất cả vận hành trên cùng một kiến trúc <strong>Laravel Backend + Saola SPA Client</strong>.
            </p>
        </div>

        <div class="ex-sections">
            {{-- Nhóm 1: Quản trị Dữ liệu & Bảng biểu --}}
            <section class="ex-group">
                <div class="ex-group-header">
                    <span class="ex-badge">01 / DỮ LIỆU LỚN &amp; CRUD</span>
                    <h2>Quản trị Dữ liệu &amp; Bảng biểu</h2>
                    <p>Giải quyết bài toán hiển thị và thao tác trên khối lượng dữ liệu lớn mà vẫn duy trì tốc độ tức thì.</p>
                </div>

                <div class="ex-cards-grid">
                    <article class="ex-card">
                        <div class="ex-card-body">
                            <span class="ex-tag">Server-side Filtering &amp; URL Sync</span>
                            <h3>Data Grid 3.800 Dòng</h3>
                            <p>Bảng dữ liệu hàng ngàn bản ghi với bộ lọc từ khóa, lọc theo trạng thái và phân trang. SSR render tức thì cho lần tải đầu, các thao tác sau chỉ lấy JSON và cập nhật DOM mượt mà, đồng bộ state lên URL.</p>
                            <div class="ex-card-specs">
                                <span><b>Dữ liệu:</b> 3.800 packages</span>
                                <span><b>Cơ chế:</b> SSR + JSON SPA update</span>
                            </div>
                        </div>
                        <div class="ex-card-footer">
                            <a class="button button-primary" href="/demo/grid">Mở ứng dụng Data Grid →</a>
                        </div>
                    </article>

                    <article class="ex-card">
                        <div class="ex-card-body">
                            <span class="ex-tag">Modal Popup &amp; Two-way Binding</span>
                            <h3>Item Editor &amp; Modal CRUD</h3>
                            <p>Hệ thống quản lý danh bạ với popup modal thêm/sửa, liên kết dữ liệu hai chiều qua <code>&#64;bind</code>, kiểm tra hợp lệ dữ liệu (validation) và cập nhật DOM tức thì với reconciliation <code>&#64;key</code>.</p>
                            <div class="ex-card-specs">
                                <span><b>Thao tác:</b> Thêm, Sửa, Xóa, Đổi thứ tự</span>
                                <span><b>Kỹ thuật:</b> Modal, &#64;bind, &#64;key</span>
                            </div>
                        </div>
                        <div class="ex-card-footer">
                            <a class="button button-primary" href="/demo/item-editor">Mở Item Editor →</a>
                        </div>
                    </article>
                </div>
            </section>

            {{-- Nhóm 2: Tương tác & Quy trình --}}
            <section class="ex-group">
                <div class="ex-group-header">
                    <span class="ex-badge">02 / WORKFLOW &amp; INTERACTION</span>
                    <h2>Tương tác Phức tạp &amp; Quy trình</h2>
                    <p>Mô hình tương tác trực quan cao độ: kéo thả giữa các danh sách và bộ đệm trạng thái client.</p>
                </div>

                <div class="ex-cards-grid">
                    <article class="ex-card">
                        <div class="ex-card-body">
                            <span class="ex-tag">Drag &amp; Drop · Cross-list Reconciliation</span>
                            <h3>Bảng Kanban Kéo Thả</h3>
                            <p>Bảng quản lý công việc nhiều cột kiểu Jira/Trello. Thẻ công việc di chuyển qua lại giữa các vòng lặp <code>&#64;foreach</code> khác nhau — ca xử lý phức tạp nhất của danh sách có khoá, đồng bộ server ngầm tối ưu.</p>
                            <div class="ex-card-specs">
                                <span><b>Tương tác:</b> Kéo thả native HTML5</span>
                                <span><b>Xử lý:</b> Optimistic UI + Server Commit</span>
                            </div>
                        </div>
                        <div class="ex-card-footer">
                            <a class="button button-primary" href="/demo/board">Mở Bảng Kanban →</a>
                        </div>
                    </article>

                    <article class="ex-card">
                        <div class="ex-card-body">
                            <span class="ex-tag">Client State &amp; PageCache LRU</span>
                            <h3>Todo Workspace</h3>
                            <p>Ứng dụng quản lý tác vụ với lọc hoàn thành, tính toán computed và lưu trữ. Đặc biệt: bảo toàn 100% form và dữ liệu khi người dùng điều hướng sang trang khác rồi bấm Back/Forward nhờ bộ đệm PageCache.</p>
                            <div class="ex-card-specs">
                                <span><b>Trạng thái:</b> State cục bộ ở instance</span>
                                <span><b>Độ bền:</b> PageCache LRU không reload</span>
                            </div>
                        </div>
                        <div class="ex-card-footer">
                            <a class="button button-primary" href="/todo-list">Mở Todo Workspace →</a>
                        </div>
                    </article>
                </div>
            </section>

            {{-- Nhóm 3: Thời gian thực & Luồng --}}
            <section class="ex-group">
                <div class="ex-group-header">
                    <span class="ex-badge">03 / REALTIME &amp; STREAMING</span>
                    <h2>Thời Gian Thực &amp; Luồng Dữ Liệu</h2>
                    <p>Giao tiếp liên tục hai chiều với máy chủ, cập nhật dữ liệu tài chính và sự kiện từng giây.</p>
                </div>

                <div class="ex-cards-grid">
                    <article class="ex-card">
                        <div class="ex-card-body">
                            <span class="ex-tag">Live Ticker &amp; Order Book</span>
                            <h3>Bảng Giá &amp; Biểu Đồ Realtime</h3>
                            <p>Bảng theo dõi thị trường tài chính với biểu đồ nến thời gian thực, sổ lệnh Order Book biến động từng mili-giây, tính toán chênh lệch giá và khớp lệnh tức thì.</p>
                            <div class="ex-card-specs">
                                <span><b>Giao diện:</b> Biểu đồ Canvas + Sổ lệnh</span>
                                <span><b>Tần suất:</b> Cập nhật liên tục theo tick</span>
                            </div>
                        </div>
                        <div class="ex-card-footer">
                            <a class="button button-primary" href="/demo/market">Mở Thị Trường Realtime →</a>
                        </div>
                    </article>

                    <article class="ex-card">
                        <div class="ex-card-body">
                            <span class="ex-tag">Server-Sent Events (SSE)</span>
                            <h3>Luồng Sự Kiện SSE Stream</h3>
                            <p>Nhận luồng tin tức liên tục từ server bằng <code>text/event-stream</code>. Tự động kết nối lại khi mất mạng, backlog SSR được render sẵn giúp trang có nội dung ngay cả khi chưa kết nối.</p>
                            <div class="ex-card-specs">
                                <span><b>Giao thức:</b> HTTP SSE EventStream</span>
                                <span><b>Đặc tính:</b> Auto-reconnect &amp; Timeline Feed</span>
                            </div>
                        </div>
                        <div class="ex-card-footer">
                            <a class="button button-primary" href="/demo/stream">Mở Luồng Sự Kiện SSE →</a>
                        </div>
                    </article>
                </div>
            </section>

            {{-- Nhóm 4: Đồng bộ Đa người dùng & Sự kiện --}}
            <section class="ex-group">
                <div class="ex-group-header">
                    <span class="ex-badge">04 / CONCURRENCY &amp; EVENTS</span>
                    <h2>Đồng Bộ Đa Người Dùng &amp; Sự Kiện</h2>
                    <p>Giải quyết bài toán nhiều người cùng chỉnh sửa dữ liệu và kiến trúc giao tiếp phân tầng.</p>
                </div>

                <div class="ex-cards-grid">
                    <article class="ex-card">
                        <div class="ex-card-body">
                            <span class="ex-tag">Concurrent Editing · Conflict Lock</span>
                            <h3>Roster Concurrent Sync</h3>
                            <p>Quản lý nhân sự đồng bộ ngầm qua API định kỳ 30s. Form nháp ở component con không bị mất khi danh sách cha refetch ngầm, hỗ trợ phát hiện xung đột phiên bản (optimistic locking).</p>
                            <div class="ex-card-specs">
                                <span><b>Đồng bộ:</b> 30s background polling</span>
                                <span><b>An toàn:</b> State isolation &amp; Version check</span>
                            </div>
                        </div>
                        <div class="ex-card-footer">
                            <a class="button button-primary" href="/roster">Mở Roster Sync →</a>
                        </div>
                    </article>

                    <article class="ex-card">
                        <div class="ex-card-body">
                            <span class="ex-tag">Child-to-Parent Event Emission</span>
                            <h3>Sự Kiện Component Con → Cha</h3>
                            <p>Kiến trúc giao tiếp giữa các tầng component: component con phát sự kiện qua event emit, component cha lắng nghe và cập nhật state tổng thể, hỗ trợ cả thẻ component tự đóng và <code>&#64;include</code>.</p>
                            <div class="ex-card-specs">
                                <span><b>Mô hình:</b> Emit dispatching</span>
                                <span><b>Cú pháp:</b> Tag event handler &amp; on$ props</span>
                            </div>
                        </div>
                        <div class="ex-card-footer">
                            <a class="button button-primary" href="/demo/emit">Mở Demo Emit Sự Kiện →</a>
                        </div>
                    </article>
                </div>
            </section>
        </div>
    @endblock
</template>

<style>
.ex-hero {
    margin-bottom: 2.5rem;
}
.ex-sections {
    display: flex;
    flex-direction: column;
    gap: 3rem;
}
.ex-group {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
}
.ex-group-header {
    border-bottom: 1px solid var(--border, rgba(0,0,0,0.08));
    padding-bottom: 0.75rem;
}
.ex-group-header h2 {
    font-size: 1.35rem;
    font-weight: 700;
    margin: 0.25rem 0;
}
.ex-group-header p {
    color: var(--text-muted, #666);
    font-size: 0.95rem;
    margin: 0;
}
.ex-badge {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.05em;
    color: var(--primary, #0284c7);
    text-transform: uppercase;
}
.ex-cards-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 1.25rem;
}
.ex-card {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    background: var(--bg-card, #ffffff);
    border: 1px solid var(--border, rgba(0,0,0,0.08));
    border-radius: 12px;
    padding: 1.5rem;
    transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
}
.ex-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0,0,0,0.06);
    border-color: var(--primary, #0284c7);
}
.ex-tag {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 600;
    background: rgba(2, 132, 199, 0.08);
    color: var(--primary, #0284c7);
    padding: 0.2rem 0.5rem;
    border-radius: 6px;
    margin-bottom: 0.75rem;
}
.ex-card-body h3 {
    font-size: 1.2rem;
    font-weight: 700;
    margin: 0 0 0.5rem 0;
}
.ex-card-body p {
    font-size: 0.9rem;
    line-height: 1.55;
    color: var(--text-secondary, #4b5563);
    margin-bottom: 1rem;
}
.ex-card-specs {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    font-size: 0.82rem;
    color: var(--text-muted, #6b7280);
    padding: 0.75rem;
    background: rgba(0,0,0,0.02);
    border-radius: 8px;
    margin-bottom: 1.25rem;
}
.ex-card-specs b {
    color: var(--text, #111827);
}
.ex-card-footer {
    padding-top: 0.5rem;
}
.ex-card-footer .button {
    width: 100%;
    text-align: center;
    justify-content: center;
}
</style>
