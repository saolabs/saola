@props({
    posts: [],
    title: null,
})
<template>
    <h1>{{ title }}</h1>
    <ul>
        @foreach(posts as post)
            <li>{{ post.title }}</li>
        @endforeach
    </ul>
    #test
</template>
