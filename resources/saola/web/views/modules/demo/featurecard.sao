@props({
    number: '01',
    title: 'Feature',
    source: '',
    tone: 'pink'
})

<template>
    <article @class(['feature-card', 'feature-card--pink': tone === 'pink', 'feature-card--cyan': tone === 'cyan', 'feature-card--yellow': tone === 'yellow'])>
        <header>
            <span>{{ number }}</span>
            <h3>{{ title }}</h3>
        </header>
        <code>{!! source !!}</code>
        <div class="feature-card__body">
            @children
        </div>
    </article>
</template>

<script setup lang="ts">
    export default { name: 'DemoFeatureCard' };
</script>

<style scoped>
    .feature-card {
        position: relative;
        border: 3px solid #1f1b16;
        background: #e8e0c0;
        box-shadow: 7px 7px 0 #1f1b16;
        color: #1f1b16;
        transform: rotate(-1deg);
    }

    .feature-card:nth-child(even) { transform: rotate(1.4deg); }
    .feature-card--cyan { box-shadow: 7px 7px 0 #00ffcc; }
    .feature-card--yellow { box-shadow: 7px 7px 0 #dfff00; }

    .feature-card > header {
        display: grid;
        grid-template-columns: 64px 1fr;
        align-items: stretch;
        border-bottom: 3px solid #1f1b16;
    }

    .feature-card > header span {
        display: grid;
        place-items: center;
        border-right: 3px solid #1f1b16;
        background: #ff71ce;
        font: 700 22px/1 Helvetica, Arial, sans-serif;
    }

    .feature-card > header h3 {
        margin: 0;
        padding: 14px 16px;
        font: 700 26px/1 Helvetica, Arial, sans-serif;
    }

    .feature-card > code {
        display: block;
        padding: 9px 14px;
        overflow-wrap: anywhere;
        border-bottom: 2px dashed #1f1b16;
        background: rgba(255, 255, 255, .24);
        font: 13px/1.45 Courier, monospace;
    }

    .feature-card__body { padding: 18px; }

    @media (max-width: 680px) {
        .feature-card,
        .feature-card:nth-child(even) { transform: none; }
    }
</style>
