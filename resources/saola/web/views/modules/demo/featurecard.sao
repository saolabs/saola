@props({
    number: '01',
    title: 'Feature',
    source: '',
    tone: 'pink'
})

<template>
    <article @class({'feature-card', 'feature-card--pink': tone === 'pink', 'feature-card--cyan': tone === 'cyan', 'feature-card--yellow': tone === 'yellow'})>
        <header>
            <span>{{ number }}</span>
            <h3>{{ title }}</h3>
        </header>
        <code>{!! source !!}</code>
        <div class="feature-card__body">
            @children
        </div>
    </article>
</template>

<script setup lang="ts">
    export default { name: 'DemoFeatureCard' };
</script>

<style scoped>
    /* Cùng bảng màu với khu docs/lab — biến lấy từ demo-site.css. */
    .feature-card {
        display: flex;
        flex-direction: column;
        gap: 10px;
        padding: 18px;
        border: 1px solid var(--line);
        border-radius: 10px;
        background: #fff;
    }
    .feature-card header { display: flex; align-items: baseline; gap: 8px; }
    .feature-card header span {
        font-size: 11px; font-weight: 700; letter-spacing: .06em;
        color: var(--muted); font-variant-numeric: tabular-nums;
    }
    .feature-card h3 { margin: 0; font-size: 15px; font-weight: 700; }
    .feature-card > code {
        align-self: flex-start;
        padding: 3px 8px; border-radius: 5px;
        background: var(--mist); color: var(--muted);
        font-size: 12px;
    }
    .feature-card__body { display: flex; flex-direction: column; gap: 10px; font-size: 14px; }
    .feature-card__body p { margin: 0; line-height: 1.6; }

    /* Tông chỉ còn là một vạch màu mảnh, không đổi cả khung. */
    .feature-card--pink { border-top: 3px solid #d93a11; }
    .feature-card--cyan { border-top: 3px solid #0e7c86; }
    .feature-card--yellow { border-top: 3px solid #b8860b; }
</style>
