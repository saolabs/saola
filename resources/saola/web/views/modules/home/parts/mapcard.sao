@props({
    eyebrow: '',
    title: '',
    href: '#',
    link: '',
    variant: ''
})

<template>
    <a @class({'map-card', 'map-card-dark': variant === 'dark'}) @attr({href: href})>
        <span>{{ eyebrow }}</span>
        <h3>{{ title }}</h3>
        @children
        <i>{{ link }}</i>
    </a>
</template>

<script setup lang="ts">
    export default { name: 'HomeMapCard' };
</script>
