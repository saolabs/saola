@states({ activeValue: 0 })

<template>
    @extends(__layout__ + "public")
    @block('content')
        <section class="page-intro page-intro-split">
            <div>
                <p class="eyebrow"><span>01</span> About Saola</p>
                <h1>Software should feel<br><em>quietly capable.</em></h1>
            </div>
            <p>We build for teams who want Laravel's reliability without giving up the immediacy people expect from modern applications.</p>
        </section>

        <section class="manifesto-grid">
            <div class="manifesto-number">01—03</div>
            <div class="value-accordion">
                <article @class(['value-item', 'open': activeValue === 0])>
                    <button @click(setActiveValue(0))><span>01</span><strong>Server truth</strong><i>+</i></button>
                    @if(activeValue === 0)
                        <p>The first response must be complete, accessible and compatible with Blade. Hydration is an enhancement, not a rescue operation.</p>
                    @endif
                </article>
                <article @class(['value-item', 'open': activeValue === 1])>
                    <button @click(setActiveValue(1))><span>02</span><strong>Small contracts</strong><i>+</i></button>
                    @if(activeValue === 1)
                        <p>Views describe UI, layouts provide durable structure, and components own focused behavior. Each layer has one job.</p>
                    @endif
                </article>
                <article @class(['value-item', 'open': activeValue === 2])>
                    <button @click(setActiveValue(2))><span>03</span><strong>Visible lifecycle</strong><i>+</i></button>
                    @if(activeValue === 2)
                        <p>Mount, pause, resume and destroy are explicit so navigation, caching and resource cleanup remain understandable.</p>
                    @endif
                </article>
            </div>
        </section>

        <section class="quote-panel"><p>“A framework earns trust when its runtime behavior matches the structure developers see in the source.”</p><span>SAOLA DESIGN PRINCIPLE / 2026</span></section>
        <section class="closing-cta"><h2>See the model in motion.</h2><a class="button button-primary" href="/todo-list">Open interactive demo <span>→</span></a></section>
    @endblock
</template>

<style scoped>
    .page-intro h1 em { color: var(--coral); font-style: normal; }
</style>
