/**
 * Bundle riêng của context `web`.
 *
 * Gộp vào `app.web.js` cùng với `resources/saola/app/` (phần dùng chung).
 */
import { defineBundle } from '@saolabs/client';

export default defineBundle({
    name: 'web',
    providers: [],
    services: {},
    helpers: {},
});
