/**
 * Bundle riêng của context `admin`.
 *
 * Gộp vào `app.admin.js` cùng với `resources/saola/app/` (phần dùng chung).
 */
import { defineBundle } from '@saolabs/client';

export default defineBundle({
    name: 'admin',
    providers: [],
    services: {},
    helpers: {},
});
