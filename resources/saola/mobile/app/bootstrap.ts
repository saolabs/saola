/**
 * Bundle riêng của context `mobile`.
 *
 * Gộp vào `app.mobile.js` cùng với `resources/saola/app/` (phần dùng chung).
 */
import { defineBundle } from '@saolabs/client';

export default defineBundle({
    name: 'mobile',
    providers: [],
    services: {},
    helpers: {},
});
